/**
 * Chat Service Interface
 * All implementations (Firebase/Supabase) must follow this structure.
 */

/*
export interface ChatService {
  // Initialization
  init(): Promise<string>; // Returns userId

  // Room Management
  joinOrCreateRoom(roomId, password, userId): Promise<void>;
  leaveRoom(roomId): Promise<void>;
  destroyRoom(roomId): Promise<void>;

  // Messaging
  sendMessage(roomId, text, senderId): Promise<void>;
  
  // Listeners
  onRoomUpdate(roomId, callback): Function; // Returns unsubscribe
  onMessageUpdate(roomId, callback): Function; // Returns unsubscribe
}
*/
